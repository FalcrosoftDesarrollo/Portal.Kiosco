<Window x:Class="Portal.Kiosco.IngresoDePassword"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Portal.Kiosco"
        mc:Ignorable="d"
        Title="IngresoDePassword" Height="1080" Width="1920"
        WindowState="Maximized"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        >
    <Border>
        <Grid>
            <Border Background="Black" Opacity="0.7" />
            <Border CornerRadius="15" Background="#2F2A29" BorderThickness="1" Width="502" Height="322" >
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition />
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>

                    <!--BtnClose-->
                    <Image Source="C:\FALCROSOFT\PROCINAL\Portal.Kiosco\Properties\Resources\close.png" Stretch="Fill" Grid.Column="2" VerticalAlignment="Bottom" Width="30" Height="30" HorizontalAlignment="left" Cursor="Hand" />

                    <!--Titulo-->
                    <Label Grid.Column="1" Grid.Row="1" Content="Autentificación" FontSize="20" FontFamily="Poppins SemiBold" Foreground="White" HorizontalAlignment="Center" Height="50" VerticalAlignment="Top"/>

                    <!--Label y Input-->
                    <Border Grid.Column="1" Grid.Row="1" Height="80" Width="400" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,60">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Label Grid.Row="0" Content="Ingrese su contraseña CineFan" FontSize="16" FontFamily="Poppins Regular" Foreground="White" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>
                            <Border Grid.Row="1" Width="400" BorderBrush="White" Background="White" BorderThickness="4" CornerRadius="6">
                                <TextBox Background="Transparent" FontFamily="Poppins Regular" FontSize="18" BorderThickness="0" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!--Boton Iniciar Sesión-->
                    <Border Grid.Column="1" Grid.Row="1" Width="400" Height="46" Background="#ED1C24" BorderThickness="1"  CornerRadius="10" VerticalAlignment="Bottom" Margin="0,0,0,50">
                        <Button Cursor="Hand" FontSize="16" FontFamily="Poppins Regular" Foreground="White"  Background="Transparent" BorderThickness="0" Content="Iniciar Sesión" HorizontalAlignment="Right" Width="400" Height="46" VerticalAlignment="Center" Margin="0,0,0,0"/>
                    </Border>

                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
